# Data Lake Development and Integration

This project demonstrates data ingestion into an AWS S3-based data lake, with data transformation from CSV to Parquet format.

## How to Run
1. Install required packages:
   ```bash
   pip install pandas boto3
